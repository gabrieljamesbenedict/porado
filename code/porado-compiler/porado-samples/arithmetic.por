num as int = 42;
otherNum as int = -10 + 5 * -1 + (++num * num--);

// Arithmetic operations
sum as int = num + 10;
diff as int = num - 5;
prod as int = num * 2;
quot as int = num / 3;
mod as int = num % 4;

// Increment / Decrement
num++;
--diff;